# src/

Contains the `tsbadparser` package — the only installable artifact in this repo.

```
src/
└── tsbadparser/     # pip-installable parser for the TSB-AD benchmark
    ├── __init__.py
    └── parser.py
```

Install from the repo root:
```bash
pip install -e .
```
